# Getting Started

Install dependencies:

```bash
bun install
```

Start the Aria Node Runtime development surface:

```bash
bun run dev:node
```

The product model is:

```text
Desktop = Aria Node Runtime + Desktop UI
Headless = same Aria Node Runtime without Desktop UI
Mobile = remote client only
```
